# Claude Code: FNDR 2.0

Follow the `fndr-v2-engineering` skill (`.claude/skills/fndr-v2-engineering/`) for every task in this repo. It routes each task to the right workflow, invariant checklist, lane contract, and verification commands. `AGENTS.md` mirrors it for other agent tools and is generated; never edit it by hand.

Plan of record: `docs/PRD.md`, `docs/decisions/` ADR-001..007, `docs/ARCHITECTURE.md`, `docs/ROADMAP-TICKETS.md`. The plan is approved: re-read it, do not re-litigate it.

Ground rules: never commit to `main` (branch, PR, merge with green CI); `make test` before claiming anything done; no em dashes in any output; session ends with work in flight write a handoff note (see CONTRIBUTING.md).
