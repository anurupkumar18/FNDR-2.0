# Handoff: T-201 schema v1 and migrations (2026-08-21)

Done (PR #8): the system-of-record Store opens WAL with foreign keys enforced and forward-only embedded migrations (user_version tracked, one transaction each, typed SchemaTooNew error instead of silent downgrade). Migration 0001 covers every ARCHITECTURE section 5 domain: split memory schema (memory_records facts, memory_texts derived, memory_scores named scores), chunks with spans, the FK-checked graph (nodes, edges, node_mentions, entity_aliases) with cascade on delete, tasks, meetings plus segments, decision_ledger, the durable review_queue, settings, devices, and hashed-only tokens. Lifecycle enums are integer discriminants in fndr-types with pinned values and a typed unknown-discriminant error.

In flight: nothing broken. Next per E02: T-202 Lance writer with batched flush (needs the T-208 spike first or alongside), T-206 deletion-everywhere rides the cascade spine laid here. Domain write/read APIs on Store land with their pipeline tickets; Store::conn is test-only until then (no unused scaffolding ships).

Decisions: record and node ids are TEXT (UUIDs; nodes UUIDv5 per the crate map). Graph kind columns are open integers until the fndr-graph taxonomy port (T-1101) pins them. ON DELETE CASCADE for derived tables and edges; SET NULL where a record is optional provenance (tasks, decision_ledger). Tokens table stores hashes only (ADR-007).

Landmines: never edit a shipped migration, append the next file; discriminant values are pinned by test and must never be renumbered. SkeletonStore still exists beside Store and dies when E02/E03 wire the real paths.

Produced by: Anurup + Claude Code
