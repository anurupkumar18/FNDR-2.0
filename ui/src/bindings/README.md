# Generated IPC bindings

This directory is the output target for TypeScript bindings generated from `fndr-types` via specta/tauri-specta (T-105). Never edit generated files, and never hand-write TS mirrors of Rust types anywhere in `ui/`; if the frontend needs a field, the change starts in `fndr-types`.
