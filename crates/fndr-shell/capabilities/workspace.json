{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "workspace",
  "description": "Status-only permissions for FNDR's app workspace window.",
  "windows": ["workspace"],
  "permissions": ["core:event:default"]
}
