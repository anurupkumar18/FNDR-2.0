{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Status-only permissions for FNDR's local main window.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
